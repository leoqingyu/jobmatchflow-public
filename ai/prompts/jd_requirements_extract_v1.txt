You are an expert JD (job description) parser for job-matching purposes.

Read the job description below and extract a flat list of atomic requirements. Split it as finely as reasonable — do not merge multiple unrelated requirements into one item — but do not manufacture requirements that aren't actually implied by the text.

For each requirement, output:
- "id": a short stable id, "r1", "r2", "r3", ... in the order you list them.
- "category": one of "skill" | "experience" | "capability" | "education" | "domain" | "language" | "work_authorization" | "certification".
  - "skill": a named technology/tool/technical skill.
  - "experience": a years-of-experience or seniority-level requirement.
  - "capability": a soft/behavioral or higher-level capability (e.g. "stakeholder management", "ability to work independently").
  - "education": a degree/field requirement.
  - "domain": industry/domain knowledge (e.g. "experience in fintech", "familiarity with AML regulations").
  - "language": a spoken/written language proficiency requirement.
  - "work_authorization": right-to-work / visa / permit / citizenship requirement.
  - "certification": a specific professional certification/license requirement.
- "text": one clear sentence stating the requirement, in English regardless of the JD's original language.
- "importance": "must" if the JD states it as required/mandatory/essential, "nice" if stated as preferred/bonus/a plus. If genuinely ambiguous, default to "must".
- "hard_constraint": ONLY set (non-null) when category is "language", "work_authorization", or "certification" AND the JD phrases it as an absolute non-negotiable blocker with no flexibility (e.g. "must already hold a Swiss work permit, no sponsorship", "native German required", "must hold an active CFA charter"). Set to the same value as category ("language"/"work_authorization"/"certification"). Otherwise omit or set to null — most language/work_authorization/certification mentions are NOT hard constraints unless phrased as an absolute blocker. If the JD explicitly says it sponsors visas / is open to relocation support, do NOT mark work_authorization as a hard constraint (or emit that requirement as a hard constraint at all).
- "hard_constraint_detail": only present when hard_constraint is set.
  - for "language": {"language": "<English name>", "min_level": "<A1|A2|B1|B2|C1|C2>"} (map "native"/"fluent"->C2, "professional working proficiency"->C1, etc).
  - for "work_authorization": {"note": "<short quote/paraphrase of the exact requirement>"}.
  - for "certification": {"note": "<certification name>"}.

Also classify the job posting as a whole (not per-requirement) into exactly ONE coarse domain bucket, "job_domain", one of:
- "tech-backend": backend/platform/infra/general software engineering.
- "tech-data": data engineering, data science, analytics, ML/AI roles.
- "finance-compliance": AML/KYC, regulatory, risk, audit, compliance roles.
- "finance-quant": quantitative trading/research, financial engineering roles.
- "cross-fintech": roles that are explicitly a blend of finance + software/data (e.g. "financial software engineer", "trading systems developer", fintech product roles) — use this instead of picking one side when the role is genuinely both.
- "other": anything that doesn't fit the above (non-tech non-finance roles, or too little signal to classify).
Pick the single best-fitting bucket; do not invent new bucket names.

Also classify the job posting's employment type, "employment_type", one of:
- "internship": internships, working student / Werkstudent roles, trainee programs, and other clearly fixed-term, structured early-career programs explicitly framed as such — even if the title doesn't literally say "intern" (e.g. "Trainee — Risk", "Praktikant"). Do NOT use this for graduate programs — see below.
- "graduate_program": postings explicitly framed as a "Graduate Programme" / "Graduate Scheme" / "Rotational Programme" or equivalent. These are ambiguous by nature — some are fixed-term like an internship, others are a permanent full-time hiring track — so classify them here rather than guessing which one, unless the posting text itself is explicit about being fixed-term (then use "internship") or a permanent hire from day one (then use "full_time").
- "full_time": everything else, including junior/entry-level permanent roles. A junior full-time hire is NOT an internship just because it's entry-level — only classify as "internship" when the posting is explicitly framed as a fixed-term training/student program, not merely low-seniority.
When genuinely ambiguous, default to "full_time" (most postings are; do not over-classify as internship or graduate_program).

Also classify the job posting's seniority level as a whole, "job_seniority", one of:
- "junior": explicit asks of 3 years of experience or less, OR entry-level/associate/graduate framing, OR a higher-year mention that reads as aspirational/nice-to-have rather than a hard bar. Treat this as a holistic read of the whole posting, not a mechanical max() of any single requirement's stated years — a junior-framed posting that casually mentions "5+ years a plus" is still "junior".
- "mid": a realistic bar of roughly 3-5 years, with no senior/lead/staff/principal framing in the title or responsibilities.
- "senior": the title explicitly contains Senior/Lead/Staff/Principal/Director, OR a >5 years requirement is stated as a hard "must" (not "nice to have"), OR the responsibilities clearly assume senior-level ownership (e.g. owning architecture decisions, mentoring, setting technical direction). Do NOT classify as "senior" purely because of a single throwaway "5+ years preferred" bullet in an otherwise junior/mid posting — look at the posting as a whole.
Pick the single best-fitting bucket; when genuinely ambiguous, default to "mid" (do not default to "senior").

Return ONLY a valid JSON object with this exact structure, no markdown, no extra text:
{
  "requirements": [
    {"id": "r1", "category": "skill", "text": "...", "importance": "must", "hard_constraint": null, "hard_constraint_detail": null},
    {"id": "r2", "category": "language", "text": "Fluent German required, no exceptions", "importance": "must", "hard_constraint": "language", "hard_constraint_detail": {"language": "German", "min_level": "C1"}}
  ],
  "job_domain": "tech-backend",
  "employment_type": "full_time",
  "job_seniority": "mid"
}

In every string value, use plain text only: do NOT use backslash except as required by JSON. No LaTeX and no Windows paths in strings.
